//$(document).ready(function(){

    //cuando el usuario baje la pantalla

  //  $(window).scroll(function(){

        //cuando el usuario utilice el scroll
    //    if( $(this).scrollTop() > 0 ){

      //      $('header').addClass ('header2');

     //   } else {
            
       //     $('header').removeClass ('header2');
    //    }

  //  });
//});